# 2026-05-10

## Today

- Moved Fursay into `/Users/mac/Documents/New project 3/fursay`.
- Set up `project-control-vault/` as the Obsidian vault.
- Opened the vault in Obsidian and confirmed `00 Home.md` is the start note.
- Confirmed the vault is named `project-control-vault` in Obsidian.

## Changes

- Added vault start page: `00 Home.md`.
- Added setup guide: `Obsidian Setup.md`.
- Added Daily Notes and Templates folders.
- Added local Obsidian settings under `.obsidian/`; these are intentionally ignored by Git.

## Render Queue

- `test-gpu-handoff-001` remains the first Windows smoke-test job.

## Blockers

- No Git remote is configured yet.
- Windows still needs the latest bundle or a future Git remote.

## Next

- Use Obsidian `Dashboard` as the current-status page.
- Use `Render Queue` before sending work to Windows.
- When ready, create or connect a Git remote so Windows can clone instead of using transfer bundles.
